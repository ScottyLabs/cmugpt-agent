[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn --chdir src --bind 0.0.0.0:$PORT --workers 2 --threads 4 --timeout 120 main:app"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
